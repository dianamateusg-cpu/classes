# Local preview config for the http.server used during editing.
# Machine-specific tooling, not part of the site.
.claude/launch.json

# macOS
.DS_Store

# Imagen original a tamano completo (se versiona solo la optimizada para web)
_concepts/img/patio.jpg
